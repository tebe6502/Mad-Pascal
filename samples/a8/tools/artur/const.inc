SCREEN_WIDTH = 40;
SCREEN_HEIGHT = 24;
SCREEN_SIZE = SCREEN_HEIGHT * SCREEN_WIDTH;
FULLSCREEN_UNDO_COUNT = 8; // each one takes 960 bytes
UNDO_ACTIONS_COUNT = 64;
BIN_HEADER_SIZE = 105;

FREE_BOTTOM = $7000;  

PMG = FREE_BOTTOM; // $7000
DISPLAY_LIST_ADDRESS = PMG;
BIN_HEADER = PMG + $100;

CUSTOM_CHARSET = PMG + $400; // $7400

VIDEO_BUFFER = CUSTOM_CHARSET + $400; // $7800  
MENU_BAR = VIDEO_BUFFER + SCREEN_SIZE; 
VIDEO_RAM_ADDRESS = VIDEO_BUFFER + $400; // $7c00 
STATUS_BAR = VIDEO_RAM_ADDRESS + SCREEN_SIZE;  
KEY_MAP = FREE_BOTTOM + $1000;        // $8000 -> $8100
UNDO_SCREENS = $8100;  // takes 960 * FULLSCREEN_UNDO_COUNT bytes  
// ends at $9F00
DEFAULT_CHARSET = $E000;

NONE = $ff;


UNDO_NONE = $ff;
UNDO_FROM_BUFFER = $fe;

MODE_TYPE = 0;
MODE_DRAW = 1;
MODE_BLOCK = 2;
MODE_LAST = 3;
MODE_EXIT = $ff;

TOOL_DRAW = 0;
TOOL_LINE = 1;
TOOL_FRAME = 2;
TOOL_BLOCK = 3;

KEY_DELAY = 10;
JOY_DELAY = 4;
FIRE_DELAY = 20;
OS_REPEAT_DELAY = 10;
OS_REPEAT_RATE = 5;

PMG_XOFFSET = 48;
PMG_YOFFSET = 15;

BG_DEFAULT = $92;
FG_DEFAULT = $9a;

MAX_BRUSH_HISTORY = 17;

CODE_DISPLAY_NONE = 0;
CODE_DISPLAY_ANTIC = 1;
CODE_DISPLAY_ATASCII = 2;
