uses crt;

var a: word;

    b: boolean;

begin

 b := (a = 0);

 b := (a <> 0);

 b := (a < 0);

 b := (a > 0);

 b := (a <= 0);

 b := (a >= 0);


 b := (a = 100);

 b := (a <> 100);

 b := (a < 100);

 b := (a > 100);

 b := (a <= 100);

 b := (a >= 100);

end.
