        var a, b, c: byte;
            [volatile] timer: byte = 0; // initialized to suppress warning

        begin
            a := timer;
            b := timer;
            c := timer;
        end.

