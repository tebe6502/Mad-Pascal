uses crt;

var ch: char;

begin

repeat until keypressed;
ch:=readkey();                              // tutaj odczytuje klawisz

writeln('1');

repeat until keypressed;
ch:=readkey();                              // tutaj odczytuje klawisz

writeln('2');

repeat until keypressed;
ch:=readkey();                              // tutaj odczytuje klawisz

writeln('3');

repeat until keypressed;
ch:=readkey();                              // tutaj odczytuje klawisz

writeln('4');

end.