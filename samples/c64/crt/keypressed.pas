uses crt;

var ch: char;

begin



repeat until keypressed;
ch:=readkey();                              // tutaj odczytuje klawisz

writeln(ch);

repeat until keypressed;
ch:=readkey();                              // tutaj odczytuje klawisz

writeln(ch);

repeat until keypressed;
ch:=readkey();                              // tutaj odczytuje klawisz

writeln(ch);

repeat until keypressed;
ch:=readkey();                              // tutaj odczytuje klawisz

writeln(ch);

end.